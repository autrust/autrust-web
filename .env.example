# Base de données (SQLite en dev, PostgreSQL en prod)
DATABASE_URL="file:./dev.db"

# URL du site (obligatoire en prod pour emails et redirects)
APP_URL="http://localhost:3000"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Stripe (paiements) — obligatoire pour checkouts et webhooks
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# Cloudflare Turnstile (anti-bots sur login, inscription, contact)
# Créer un widget sur https://dash.cloudflare.com → Turnstile
# Si non définis : le widget ne s'affiche pas et la vérification est désactivée
NEXT_PUBLIC_TURNSTILE_SITE_KEY=""
TURNSTILE_SECRET_KEY=""

# Optionnel : assistant chat (OpenAI)
# OPENAI_API_KEY="sk-..."

# Optionnel : envoi d’emails (Resend, ex. rapports CarVertical)
# RESEND_API_KEY="re_..."
# RESEND_FROM_EMAIL="AuTrust <noreply@autrust.be>"

# Optionnel : admin (emails et téléphones autorisés à accéder à /admin)
# ADMIN_EMAILS="admin@example.com"
# ADMIN_PHONES="+32..."
# ADMIN_SKIP_PHONE_VERIFICATION="false"
